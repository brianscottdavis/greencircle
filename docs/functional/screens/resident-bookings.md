# Screen Spec: Resident Bookings (Bulky Pickup)
Route: `/{locale}/dashboard/bookings`

Create bulky pickup request:
- RequestType = BulkyPickup
- Item list + preferred date options from schedule rules.
